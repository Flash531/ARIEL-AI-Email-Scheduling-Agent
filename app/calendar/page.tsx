// Calendar page — temporarily disabled
// Uncomment the full implementation when ready to enable

import { redirect } from "next/navigation";
export default function CalendarPage() { redirect("/"); }
